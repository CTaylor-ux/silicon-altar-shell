# Absolute path to the Silicon Altar audit repo.
# The shell READS from it and never writes to it.
SILICON_ALTAR_REPO=/Users/taylorcolin/Desktop/Silicon_Altar_LIVE

# FUTURE: retrieval backend credentials live here once lib/retrieval.ts stops
# being a stub. Nothing in this pass needs a key.
